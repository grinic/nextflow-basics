
// conda.enabled = true

// Make sure Docker doesn't use root.
process.container = 'ngritti/pymage-training', options = '-v /home/:/home/'
// docker.sudo = true
process.containerOptions = { workflow.containerEngine == "docker" ? '-u $(id -u):$(id -g)': null}

// Where to cache the singularity.
// singularity.cacheDir = ...
